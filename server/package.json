{
  "name": "hemplife-server",
  "version": "1.0.0",
  "description": "Hemp Life Farmers — API server",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "db:init": "node -e \"require('./lib/db').query(require('fs').readFileSync('./schema.sql','utf8')).then(()=>{console.log('Schema applied');process.exit()}).catch(e=>{console.error(e);process.exit(1)})\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "pg": "^8.13.0",
    "resend": "^4.0.0",
    "uuid": "^10.0.0"
  }
}
